<div class="cs-apply-damage-wrapper">
    <button type="button" class="cs-apply-damage-button"
            data-message-id="{{messageId}}"
            data-actor-id="{{actorId}}"
            data-weapon-id="{{weaponId}}"
            data-weapon-name="{{weaponName}}"
            data-roll-total="{{rollTotal}}">
        {{localize "CS.chat.applyDamage.button"}}
    </button>
</div>
